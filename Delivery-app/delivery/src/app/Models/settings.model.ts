export interface Settings {
    notifications: boolean; // Example: Enable or disable notifications
    language: string; // Example: Language preference
    theme: string; // Example: App theme preference
    // Add more settings as needed
  }